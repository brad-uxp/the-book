# Copy this file to .env and fill in the values

# Database (required)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/database?sslmode=require"

# Vercel Cron secret (optional)
CRON_SECRET=""

# Soft admin key (optional, disabled when empty)
ADMIN_KEY=""
